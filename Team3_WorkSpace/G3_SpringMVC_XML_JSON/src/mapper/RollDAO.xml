<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.RollDAO">
	<insert id = "insertRoll" parameterType="vo.Roll">
		INSERT INTO roll VALUES(#{userid},'ROLE_USER')
	</insert>
</mapper>